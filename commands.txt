git merge --allow-unrelated-histories sasikanth


git pull origin branch_name --allow-unrelated-histories



sivadurgadevi@greemus.com


SELECT DISTINCT c.customer_name
FROM customers c
JOIN orders o ON c.customer_id = o.customer_id
JOIN products p ON o.product_id = p.product_id
WHERE p.price > 500;



select name, count(year) as placed_order from products where year=2024 group by name;